<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Todos</title>
</head>
<body>
    <div class="container">
        <p>&nbsp;</p>
        <p>&nbsp;</p>
        <h1>FullStack Todo Template Project</h1>
        <hr>
        <div class="rows">
            <h2>TODO LIST</h2>
            <p>Count: <%= count %></p>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Title</th>
                        <th>Description</th>
                        <th>Priority</th>
                        <th>isDone</th>
                        <th>CreatedAt</th>
                        <th><a href="/view/create" class="btn btn-primary">CREATE</a></th>
                    </tr>
                </thead>
                <tbody>
                    <% for (let todo of todos) { %>
                        <tr>
                            <td># <%= todo.dataValues.id %></td>
                            <td><%= todo.dataValues.title %></td>
                            <td><%= todo.dataValues.description %></td>
                            <td><%= priorities[todo.dataValues.priority] %></td>
                            <td><%- (todo.dataValues.isDone ? '<span class="text-success">YES</span>' : '<span class="text-danger">NO</span>') %></td>
                            <td><%= todo.dataValues.createdAt.toLocaleDateString('tr') %></td>
                            <td>
                                <a class="btn btn-sm btn-secondary" href="/view/<%= todo.dataValues.id %>">Read</a>
                                <a class="btn btn-sm btn-primary" href="/view/<%= todo.dataValues.id %>/update">Update</a>
                                <a class="btn btn-sm btn-danger" href="/view/<%= todo.dataValues.id %>/delete" onclick="confirm('Are you sure?')">Delete</a>
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
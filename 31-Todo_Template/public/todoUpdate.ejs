<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Todos</title>
</head>
<body>
    <div class="container">
        <p>&nbsp;</p>
        <p>&nbsp;</p>
        <h1>FullStack Todo Template Project</h1>
        <hr>
        <div class="rows">
            <h2>TODO UPDATE</h2>
            <p><a href="/view" class="btn btn-link">Todo List</a></p>
            <form action="" method="POST">
                <table class="table table-striped">
                    <tr>
                        <td>isDone:</td>
                        <td><select class="form-select" name="isDone">
                            <option value="1" <%= (todo.isDone ? 'selected' : '') %>>YES</option>
                            <option value="0" <%= (todo.isDone ? '' : 'selected') %>>NO</option>
                        </select></td>
                    </tr>
                    <tr>
                        <td>Priority:</td>
                        <td><select class="form-select" name="priority">
                            <% for (let key in priorities) { %>
                                <option value="<%= key %>" <%= (todo.priority == key ? 'selected' : '') %>><%= priorities[key] %></option>
                            <% } %>
                        </select></td>
                    </tr>
                    <tr>
                        <td>Title:</td>
                        <td><input type="text" class="form-control" name="title" value="<%= todo.title %>" /></td>
                    </tr>
                    <tr>
                        <td>Description:</td>
                        <td><textarea class="form-control" name="description"><%= todo.description %></textarea></td>
                    </tr>
                </table>
                <p>
                    <button class="btn btn-primary" type="submit">CREATE</button>
                </p>
            </form>
        </div>
    </div>
</body>
</html>